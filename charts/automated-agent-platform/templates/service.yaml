apiVersion: v1
kind: Service
metadata:
  name: {{ include "automated-agent-platform.fullname" . }}
  labels:
    {{- include "automated-agent-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "automated-agent-platform.selectorLabels" . | nindent 4 }}

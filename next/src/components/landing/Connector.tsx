import React from "react";

const Connector = () => {
  return (
    <div className="flex h-screen w-screen items-center justify-center bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-slate-950 from-5% via-transparent via-50% to-transparent "></div>
  );
};

export default Connector;
